services:
  weatherapp:
    build: .
    env_file: .env
    expose:
      - "${PORT}"
    restart: unless-stopped

  caddy:
    image: caddy:latest
    volumes:
      - /vagrant/Caddyfile:/etc/caddy/Caddyfile
    ports:
      - "443:443"
    depends_on:
      - weatherapp
    restart: unless-stopped
